.DS_Store

# local telemetry — the records themselves ARE meant to be committed
.whence/surfaced.jsonl

# the binary, built in place by the README's install step
/whence

# site build
web/node_modules/
web/dist/

# working notes, not part of the tool
HANDOFF.md

# local editor and agent config — one machine's setup, not the tool's
.cursor/
opencode.json
.DS_Store
.mcp.json
